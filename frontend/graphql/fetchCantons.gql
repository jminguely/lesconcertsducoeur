query getCantons($locale: String) {
  cantons(locale: $locale) {
    id
    Headline {
      Image {
        id
        url
      }
    }
    abbreviation
    name
    locale
    localizations(where: { locale: "fr-CH" }) {
      id
    }
  }
}
